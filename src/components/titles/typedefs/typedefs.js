import { TowerDefenseScene } from "../../../scenes/towerDefenseScene";

/**
 * @typedef {object} TitleConfig
 * @property {TowerDefenseScene} scene The Scene to which this Game Object 
 * belongs. It can only belong to one Scene at any given time.
 * @property {number} x The x coordinate of this Game Object in world space.
 * @property {number} y The y coordinate of this Game Object in world space.
 * @property {string} text The string, or array of strings, to be set as the 
 * content of this Bitmap Text.
 * @property {Phaser.Types.GameObjects.Text.TextStyle} [style] The text style configuration object. Default is TextStyleEnum.SmallTitle.
 */